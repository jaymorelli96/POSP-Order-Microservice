pipeline { 
    agent any 

    stages { 
		stage('build') {
			steps {
				bat 'mvn clean install -DskipTests'
			}
		}
		stage('jacoco') {
			steps {
			jacoco()
			}
		}		
		
	}


	post {
		always {
			cleanWs()
		}
	}

}
