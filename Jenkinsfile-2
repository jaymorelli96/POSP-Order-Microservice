pipeline { 
    agent any 

    stages { 
		stage('build') {
			steps {
				bat 'mvn clean install'
			}
		}
		stage('jacoco') {
			steps {
			jacoco()
			}
		}		
		
	}


	post {
		always {
			cleanWs()
		}
	}

}
