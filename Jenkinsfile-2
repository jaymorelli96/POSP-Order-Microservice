pipeline { 
    agent any 

    stages { 
		stage('build') {
			steps {
				bat 'mvn clean compile'
			}
		}
		stage('jacoco') {
			steps {
			jacoco()
			}
		}		
		
	}


	post {
		always {
			cleanWs()
		}
	}

}
